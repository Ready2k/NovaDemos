{
    "name": "get_server_time",
    "description": "Gets the current date and time. Can optionally specify a timezone (UTC, EST, PST, GMT, CET, JST). Uses AgentCore Gateway for real-time accuracy.",
    "agentCoreToolName": "get-Time___get_current_time",

    "input_schema": {
        "type": "object",
        "properties": {
            "timezone": {
                "type": "string",
                "description": "Optional timezone (UTC, EST, PST, GMT, CET, JST). Defaults to UTC if not specified.",
                "enum": ["UTC", "EST", "PST", "GMT", "CET", "JST"]
            }
        },
        "required": []
    }
}