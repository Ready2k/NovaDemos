{
  "name": "disputes_agent",
  "description": "Handles card and payment disputes for UK retail banking customers. Use when the customer mentions a disputed transaction, chargeback, or unauthorized payment.",
  "input_schema": {
    "type": "object",
    "properties": {
      "customerId": {
        "type": "string",
        "description": "Internal customer identifier"
      },
      "utterance": {
        "type": "string",
        "description": "Customer's raw request text extracted from their speech"
      },
      "locale": {
        "type": "string",
        "description": "Locale code, e.g., en-GB"
      }
    },
    "required": [
      "customerId",
      "utterance"
    ]
  }
}