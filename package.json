{
    "name": "voice-s2s-monorepo",
    "version": "1.0.0",
    "description": "Voice S2S Real-Time Assistant Monorepo",
    "private": true,
    "scripts": {
        "install:all": "npm install && npm install --prefix frontend-v2 && npm install --prefix backend",
        "build:frontend": "npm run build --prefix frontend-v2",
        "build:backend": "npm run build --prefix backend",
        "build": "npm run build:frontend && npm run build:backend",
        "start": "npm run start --prefix backend"
    },
    "dependencies": {}
}